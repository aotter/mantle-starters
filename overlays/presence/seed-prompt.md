# Presence seed prompt

Create short homepage copy for `{{BRAND}}` in the canonical locale.
Keep the first screen useful without requiring images. The result should
fit the `page` Schema with one `home` page and these sections: hero,
features, about, cta, and contact.

Also create one sample `contact` draft that demonstrates the expected
message shape. Keep all copy easy to replace by the site owner.
