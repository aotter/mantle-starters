# Reservation overlay

Use this when the launch type intent is `reservation`.

First useful shape:

- capture reservation requests with `/api/reservation-requests`;
- show staff the recent requests View;
- avoid calendar sync, payments, deposits, seat maps, and notification
  automation until the user confirms that workflow.
