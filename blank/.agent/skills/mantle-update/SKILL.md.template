---
name: mantle:update
description: Compare this generated Mantle site against its deterministic upstream provision bundle.
---

# Mantle Update

Use this when the user asks to check upstream starter drift.

## Workflow

1. Start from a clean git worktree.
2. Run a deterministic bundle comparison:

```bash
pnpm run mantle:update -- --ref <starters-ref>
```

3. Read `.mantle/update-report.json`.
4. Apply useful differences manually. Do not blindly overwrite
   user-owned code.
5. Re-run:

```bash
pnpm validate
pnpm typecheck
```

## Boundary

`mantle:update` only compares files from the launch archetype's
`provision-bundles/<type>.json`.
It never applies changes, installs dependencies, or calls the old
create-mantle scaffolder.
