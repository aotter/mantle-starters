import type { KvCache, KvListResult, KvPutOptions } from "@aotterclam/clam-cms-runtime";
/**
 * `KvCache` impl wrapping Cloudflare Workers KV. Pure pass-through —
 * the port shape is a strict subset of the KV namespace API.
 */
export declare class KvCacheBinding implements KvCache {
    private readonly kv;
    constructor(kv: KVNamespace);
    get(key: string): Promise<string | null>;
    put(key: string, value: string, opts?: KvPutOptions): Promise<void>;
    delete(key: string): Promise<void>;
    list(prefix: string, cursor?: string | null): Promise<KvListResult>;
}
//# sourceMappingURL=KvCacheBinding.d.ts.map