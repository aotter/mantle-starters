import type { SiteDefaults } from "../model/SiteConfig.js";
/**
 * Synchronous fail-fast for `siteDefaults`. Throws
 * `InvalidSiteDefaultsError` when any declared locale fails BCP 47
 * canonicalization. Brand / title / description / origin are not
 * validated — only the locale tags carry semantics that the runtime
 * depends on.
 *
 * Lives in spec (not runtime) because it's pure validation against
 * the `SiteConfig` contract — no env, no DB. Runtime calls this at
 * module init (`bootInit`) so a typo in `clam.config.ts >
 * siteDefaults.locales` surfaces in `wrangler tail` before the
 * worker accepts traffic.
 */
export declare class InvalidSiteDefaultsError extends Error {
    readonly invalidLocales: ReadonlyArray<string>;
    constructor(invalidLocales: ReadonlyArray<string>);
}
export declare function assertSiteDefaultsCanonical(defaults: SiteDefaults | undefined): void;
//# sourceMappingURL=SiteDefaultsValidator.d.ts.map