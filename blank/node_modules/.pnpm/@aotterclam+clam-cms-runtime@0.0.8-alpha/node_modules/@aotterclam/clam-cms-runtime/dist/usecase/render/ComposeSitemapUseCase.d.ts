import type { DatabaseDriver } from "../../domain/port/DatabaseDriver.js";
import type { ComposeSitemapRequest } from "../dto/render/ComposeSitemapRequest.js";
/**
 * Compose `sitemap.xml` from every published entry across all
 * collections + locales. Result is XML text; consumer routes it as
 * `application/xml`. See ComposeSitemapRequest for the pathFor +
 * maxUrls knobs.
 */
export declare const SITEMAP_MAX_URLS_DEFAULT = 50000;
export declare class ComposeSitemapUseCase {
    private readonly db;
    constructor(db: DatabaseDriver);
    execute(request: ComposeSitemapRequest): Promise<string>;
}
//# sourceMappingURL=ComposeSitemapUseCase.d.ts.map