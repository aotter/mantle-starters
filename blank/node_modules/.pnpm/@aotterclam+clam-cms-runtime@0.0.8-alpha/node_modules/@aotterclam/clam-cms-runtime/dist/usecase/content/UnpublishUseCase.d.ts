import type { EntryRow } from "../../domain/model/EntryRow.js";
import type { Clock } from "../../domain/port/Clock.js";
import type { EntryRepository } from "../../domain/port/EntryRepository.js";
import type { UnpublishRequest } from "../dto/content/index.js";
import { type ContentPublishEffects } from "./ContentPublishEffects.js";
/**
 * `UnpublishUseCase` — flip published / archived rows back to
 * `'draft'`. Used by the admin SPA's "edit a published entry" flow.
 */
export declare class UnpublishUseCase {
    private readonly entries;
    private readonly clock;
    private readonly effects?;
    constructor(entries: EntryRepository, clock: Clock, effects?: ContentPublishEffects | undefined);
    execute(request: UnpublishRequest): Promise<EntryRow>;
}
//# sourceMappingURL=UnpublishUseCase.d.ts.map