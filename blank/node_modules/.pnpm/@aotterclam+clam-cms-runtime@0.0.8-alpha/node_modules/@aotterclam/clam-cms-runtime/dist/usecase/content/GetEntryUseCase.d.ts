import type { EntryRow } from "../../domain/model/EntryRow.js";
import type { EntryRepository } from "../../domain/port/EntryRepository.js";
import type { GetEntryRequest } from "../dto/content/index.js";
/**
 * `GetEntryUseCase` — fetch an entry by id, optionally asserting its
 * collection matches.
 */
export declare class GetEntryUseCase {
    private readonly entries;
    constructor(entries: EntryRepository);
    execute(request: GetEntryRequest): Promise<EntryRow>;
}
//# sourceMappingURL=GetEntryUseCase.d.ts.map