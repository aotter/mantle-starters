import { type Diagnostic } from "@aotterclam/clam-cms-spec";
export declare function jsonRpcOk(id: unknown, result: unknown): Response;
export declare function jsonRpcOkRaw(id: unknown, resultJson: string): Response;
export declare function jsonRpcError(id: unknown, code: number, message: string, data?: unknown): Response;
/**
 * Build a JSON-RPC error `Response` carrying a structured Diagnostic.
 * Used by transport adapters at the layer outside the dispatcher for
 * HTTP-level rejections (missing bearer token, non-staff caller) that
 * never enter the JSON-RPC dispatcher's own catch path.
 */
export declare function mcpJsonRpcError(diagnostic: Diagnostic, requestId?: unknown): Response;
/** Helper to construct an "unauthenticated" MCP error response. */
export declare function mcpUnauthenticated(message: string): Response;
//# sourceMappingURL=McpResponses.d.ts.map