import { type SchemaManifest } from "@aotterclam/clam-cms-spec";
import type { EntryRow } from "../../domain/model/EntryRow.js";
import type { Clock } from "../../domain/port/Clock.js";
import type { EntryRepository } from "../../domain/port/EntryRepository.js";
import type { SiteConfigRepository } from "../../domain/port/SiteConfigRepository.js";
import type { UpdateDraftRequest } from "../dto/content/index.js";
/**
 * `UpdateDraftUseCase` — update a draft's data. Only entries in
 * `'draft'` status are editable — `published` / `archived` go via the
 * unpublish (back to draft) path first.
 */
export declare class UpdateDraftUseCase {
    private readonly entries;
    private readonly schemas;
    private readonly clock;
    private readonly siteConfig?;
    constructor(entries: EntryRepository, schemas: ReadonlyMap<string, SchemaManifest>, clock: Clock, siteConfig?: SiteConfigRepository | undefined);
    execute(request: UpdateDraftRequest): Promise<EntryRow>;
}
//# sourceMappingURL=UpdateDraftUseCase.d.ts.map