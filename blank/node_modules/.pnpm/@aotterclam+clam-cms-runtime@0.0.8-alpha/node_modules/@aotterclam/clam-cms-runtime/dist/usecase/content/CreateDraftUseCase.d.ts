import { type SchemaManifest } from "@aotterclam/clam-cms-spec";
import type { EntryRow } from "../../domain/model/EntryRow.js";
import type { Clock } from "../../domain/port/Clock.js";
import type { EntryRepository } from "../../domain/port/EntryRepository.js";
import type { IdGenerator } from "../../domain/port/IdGenerator.js";
import type { SiteConfigRepository } from "../../domain/port/SiteConfigRepository.js";
import type { CreateDraftRequest } from "../dto/content/index.js";
/**
 * `CreateDraftUseCase` — create a new draft entry. Both `simple` and
 * `editorial` lifecycles start in `'draft'`; the difference shows up
 * at publish time.
 */
export declare class CreateDraftUseCase {
    private readonly entries;
    private readonly schemas;
    private readonly clock;
    private readonly idgen;
    private readonly siteConfig?;
    constructor(entries: EntryRepository, schemas: ReadonlyMap<string, SchemaManifest>, clock: Clock, idgen: IdGenerator, siteConfig?: SiteConfigRepository | undefined);
    execute(request: CreateDraftRequest): Promise<EntryRow>;
}
//# sourceMappingURL=CreateDraftUseCase.d.ts.map