import { type SchemaManifest } from "@aotterclam/clam-cms-spec";
import type { EntryRow } from "../../domain/model/EntryRow.js";
import type { Clock } from "../../domain/port/Clock.js";
import type { EntryRepository } from "../../domain/port/EntryRepository.js";
import type { ArchiveRequest } from "../dto/content/index.js";
import { type ContentPublishEffects } from "./ContentPublishEffects.js";
/**
 * `ArchiveUseCase` — flip an entry to `'archived'`. Per the simple-
 * lifecycle state machine, draft and published rows can both be
 * archived; archived rows go back to draft via `Unpublish`.
 */
export declare class ArchiveUseCase {
    private readonly entries;
    private readonly schemas;
    private readonly clock;
    private readonly effects?;
    constructor(entries: EntryRepository, schemas: ReadonlyMap<string, SchemaManifest>, clock: Clock, effects?: ContentPublishEffects | undefined);
    execute(request: ArchiveRequest): Promise<EntryRow>;
}
//# sourceMappingURL=ArchiveUseCase.d.ts.map