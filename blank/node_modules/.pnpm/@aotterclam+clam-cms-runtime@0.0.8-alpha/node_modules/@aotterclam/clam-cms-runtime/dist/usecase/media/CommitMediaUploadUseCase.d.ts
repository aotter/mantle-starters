import type { Clock } from "../../domain/port/Clock.js";
import type { KvCache } from "../../domain/port/KvCache.js";
import type { MediaAsset, MediaStorage } from "../../domain/port/MediaStorage.js";
import type { CommitMediaUploadRequest } from "../dto/media/index.js";
export declare class CommitMediaUploadUseCase {
    private readonly storage;
    private readonly kv;
    private readonly clock;
    private readonly opts;
    constructor(storage: MediaStorage, kv: KvCache, clock: Clock, opts?: {
        readonly maxBytes?: number;
    });
    execute(request: CommitMediaUploadRequest): Promise<MediaAsset>;
}
//# sourceMappingURL=CommitMediaUploadUseCase.d.ts.map