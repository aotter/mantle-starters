export async function composeSeoIfPathed(composer, paths, entry, site) {
    if (!paths)
        return undefined;
    if (!paths.forEntry(entry))
        return undefined;
    return composer.execute({ entry, site, paths });
}
//# sourceMappingURL=EntrySeoSupport.js.map