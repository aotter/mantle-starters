export declare function absoluteUrl(origin: string, path: string): string;
/** Append `.md` before any query string / hash. Used by the SEO
 *  composer + sitemap to derive markdown-mirror URLs without
 *  mangling query params. */
export declare function appendMarkdownExt(absoluteOrPath: string): string;
//# sourceMappingURL=AbsoluteUrl.d.ts.map