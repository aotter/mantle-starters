import { type SchemaManifest } from "@aotterclam/clam-cms-spec";
import type { EntryRow } from "../../domain/model/EntryRow.js";
import type { EntryRepository } from "../../domain/port/EntryRepository.js";
import type { ListEntriesRequest } from "../dto/content/index.js";
/**
 * `ListEntriesUseCase` — list entries in a collection, optionally
 * filtered by status. Asserts the collection is a declared Schema.
 *
 * Caller-supplied `limit` is clamped at this layer (the trust
 * boundary between MCP / admin transports and the chokepoint
 * `EntryRepository`).
 */
export declare class ListEntriesUseCase {
    private readonly entries;
    private readonly schemas;
    constructor(entries: EntryRepository, schemas: ReadonlyMap<string, SchemaManifest>);
    execute(request: ListEntriesRequest): Promise<readonly EntryRow[]>;
}
//# sourceMappingURL=ListEntriesUseCase.d.ts.map